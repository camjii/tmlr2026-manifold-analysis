global:
  delta_token: 0
  debug: false
  frac: 1
  use_quantization: false

grid:
  model_name:
    - meta-llama/Llama-3.2-3B-Instruct

local:
  - datasets:
    - filename: datasets/templates/notable_3way_number.csv
      label_columns: [correct_date, correct_date_expr]
      extra_columns: [correct_date_expr]
